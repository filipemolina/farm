import {
	CONSTRUIR
} from '../actions'

const initialState = [
	{ nome: "" }
]

export const construcoesReducer = (state=initialState, action) => {
	switch(action.type){
		default:
			return state
	}
}